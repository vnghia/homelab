name: homelab
description: My homelab IaC configuration
runtime:
  name: python
  options:
    virtualenv: .venv
    typechecker: mypy
config:
  pulumi:tags:
    value:
      pulumi:template: python
  server:
    value:
      platform: amd64
  docker:
    value:
      image:
        tailscale:
          repo: tailscale/tailscale
          tag: "v1.78.3"

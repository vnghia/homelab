[project]
name = "homelab-hatchet-config"
version = "0.1.0"
authors = [{ name = "Vo Van Nghia", email = "git@vnghia.com" }]
requires-python = ">=3.14.4"
dependencies = ["homelab-pydantic", "homelab-docker"]

[build-system]
requires = ["uv_build"]
build-backend = "uv_build"

[tool.ruff]
extend = "../../../pyproject.toml"

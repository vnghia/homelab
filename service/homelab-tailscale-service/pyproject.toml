[project]
name = "homelab-tailscale-service"
version = "0.1.0"
authors = [{ name = "Vo Van Nghia", email = "git@vnghia.com" }]
requires-python = ">=3.13.1"
dependencies = [
    "homelab-pydantic",
    "homelab-network",
    "homelab-docker",
    "pulumi>=3.148.0",
    "pulumi-tailscale>=0.17.5",
    "pydantic>=2.10.6",
    "httpx>=0.28.1",
]

[build-system]
requires = ["uv_build"]
build-backend = "uv_build"

[tool.ruff]
extend = "../../pyproject.toml"

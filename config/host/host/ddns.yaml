docker:
  images:
    remote:
      ddns:
        repo: ghcr.io/qdm12/ddns-updater
        tag: "v2.10.0"
  volumes:
    local:
      ddns-data: {}
services:
  ddns:
    variables:
      PORT: 8000
    config:
      traefik:
        dynamic:
          traefik:
            service:
              port:
                svariable: PORT
    container:
      image: ddns
      healthcheck: {}
      network:
        bridge:
          direct: {}
      volumes:
        ddns-data: /updater/data
      wait_timeout: 300
      envs:
        LISTENING_ADDRESS:
          extract:
            svariable: PORT
          transform:
            string:
              template: ":{value}"
        DATADIR:
          volume: ddns-data
        RESOLVER_ADDRESS: "1.1.1.1:53"

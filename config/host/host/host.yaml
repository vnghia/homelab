variables:
  FULL_HOSTNAME:
    extract:
      kv:
        project:
          extract:
            project:
          transform:
            string:
              template: "{project}-{stack}"
        host:
          hinfo: name
    transform:
      string:
        template: "{project}-{host}"
users:
  root:
    uid: 0
    gid: 0
  user: &default
    uid: 1000
    gid: 1000
  default: *default
  docker:

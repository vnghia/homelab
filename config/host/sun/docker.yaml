docker:
  volumes:
    local:
      docker-proxy-mtls:
        active: false
services:
  traefik:
    container:
      volumes:
        docker-proxy-mtls:
          active: false
  docker:
    config:
      traefik:
        proxy:
          active: false
          record: sun
    files: null
    secrets: null

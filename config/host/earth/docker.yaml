services:
  docker:
    config:
      traefik:
        proxy:
          record: earth
    secrets:
      ca-cert:
        subject:
          common-name:
            record: earth
      server-cert:
        subject:
          common-name:
            record: earth
      client-cert:
        subject:
          common-name:
            record: earth
    containers:
      proxy:
        active: true

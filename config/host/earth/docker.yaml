services:
  docker:
    config:
      traefik:
        proxy:
          record: earth
    secrets:
      proxy-mtls:
        hostname:
          record: earth
    containers:
      proxy:
        active: true
